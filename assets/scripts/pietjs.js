let canvas = document.getElementById("myCanvas");
let ctx = canvas.getContext("2d");

let size = window.innerWidth; //this line of code gets the inner width of the browser window in pixels
let dpr = window.devicePixelRatio;

//Generate(){}
